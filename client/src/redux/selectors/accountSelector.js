export const selectIsAuthenticated = (state) => state.account.isAuthenticated;
export const selectIsSpotifyAuth = (state) => state.account.isSpotifyAuth;
export const selectUsername = (state) => state.account.username;
